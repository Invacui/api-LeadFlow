// =====================
// Enums
// =====================
export enum Role {
    ADMIN,
    USER
  }
  
 export enum Plan {
    FREE,
    PRO,
    COPILOT
  }
  
 export enum PaymentStatus {
    PENDING,
    SUCCESS,
    FAILED
  }